Every example is a single file in the repository. Clone it, run `npm install && npm run build` once, then run any of them with `node examples/<file>`.

Each one imports `@b9g/termdom` exactly as your own code would — erasable TypeScript only, so they run unchanged on Node, Bun and Deno.
